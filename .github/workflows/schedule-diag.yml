name: Schedule Diagnostics

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 诊断心跳
        run: |
          echo "schedule ping @ $(date -u)"
          echo "若此步骤在无人手动触发时出现，说明 GitHub schedule 机制正常工作。"
